[project]
name = "easy-use-anima"
version = "0.12.2"
description = "Standalone Anima loader, sampler, prompt saver, and dynamic text utility with optional Easy-Use integration."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "GPL-3.0-only" }
dependencies = ["piexif"]
